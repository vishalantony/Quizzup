
<%= form_for(current_user.topic_follow_relationships.find_by(followed_topic_id: @topic.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>